#include "stdc++.h"
#include "tool.h"

#define pb push_back
#define fi first
#define se second
#define mp make_pair
#define REP(i,n) for(int (i)=0;(i)<(int)(n);(i)++)

using namespace std;

typedef pair<int, int> PII;
typedef long long LL;

template <typename T> bool chkMax(T &x, T y) { return (y > x) ? x = y, 1 : 0; }
template <typename T> bool chkMin(T &x, T y) { return (y < x) ? x = y, 1 : 0; }

class Solution {
public:

};

int main() {
//    print_code_s("[[0,3],[3,1],[1,2],[2,0]]");
//    vector<vector<int>> arr = {{0,3},{3,1},{1,2},{2,0}};
    Solution s;
//    cout << s.nearestPalindromic("1000");
    return 0;
}


